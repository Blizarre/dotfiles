# How to install

Fish has to be installed

```
git clone --separate-git-dir=$HOME/.dotfiles git@github.com:Blizarre/dotfiles.git $HOME/dotfiles
cp -r $HOME/dotfiles/.config/fish $HOME/.config
config config status.showUntrackedFiles no
rm -r $HOME/dotfiles
```
